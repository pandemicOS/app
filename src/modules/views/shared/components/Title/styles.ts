import { StyleSheet } from 'react-native';

import { STYLE } from '../../../../../constants';

export default StyleSheet.create({
  title: {
    color: STYLE.COLOR.BLACK,
    fontSize: STYLE.FONT.SIZE.HEADING_BIG,
    fontWeight: STYLE.FONT.WEIGHTS.MEDIUM,
  }
});
